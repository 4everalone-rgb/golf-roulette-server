{
  "name": "golf-game-server",
  "version": "1.0.0",
  "private": true,
  "main": "index.ts",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "echo 'No build needed'",
    "start": "node --import tsx index.ts"
  },
  "dependencies": {
    "socket.io": "^4.8.1",
    "tsx": "^4.7.0"
  }
}
