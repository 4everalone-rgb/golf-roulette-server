{
  "name": "golf-game-server",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npx tsx index.ts --version 2>/dev/null; echo build_done",
    "start": "node server.js"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}
